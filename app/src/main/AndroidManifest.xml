<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ba.sum.fpmoz.drustvenamreza">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/background_gradient"
        android:label="@string/app_name"
        android:roundIcon="@drawable/background_gradient"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">

        <activity
            android:name=".ui.theme.data.model.CommentsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.data.AddPostActivity"
            android:exported="false" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <!-- LOGIN ACTIVITY POSTAJE PRVA -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- WelcomeActivity uklonjena iz startanja aplikacije -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true" />

        <activity
            android:name=".SearchActivity"
            android:exported="false" />
        <activity android:name=".UserProfileActivity" />

    </application>

</manifest>
